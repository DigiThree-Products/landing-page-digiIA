# ATENÇÃO: este repositório é PÚBLICO.
#
# A pasta material/ tem conteúdo interno que não pode ser publicado:
#  - DigiThree_Lancamento_DigiLab.docx — o plano de lançamento completo
#    (preços, estratégia das sete semanas, nomes dos clientes do beta)
#  - Identidade Visual - Digi IA.pdf — manual de marca, 95 MB
#    (acima do limite de 100 MB do GitHub e de uso interno)
#  - PDFs de logo e ativos de origem
#
# Os arquivos que a página realmente precisa ficam versionados na raiz.
material/

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.code-workspace

# Ferramentas locais
.claude/
node_modules/
# Rascunho de execução de planos (ledger, briefs, pacotes de review).
# O registro que importa é o histórico do git; isto é descartável.
.superpowers/

# Build do Next.js — gerado, nunca versionado
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Variáveis de ambiente locais.
# O .env.example fica versionado; os valores reais, não.
.env
.env*.local

# Snapshots e logs que o Playwright MCP grava na raiz a cada navegação.
# Some e volta sozinho; numa árvore com duas sessões, arquivo não rastreado
# não tem dono e vira ruído no `git status` de quem não o criou.
.playwright-mcp/
